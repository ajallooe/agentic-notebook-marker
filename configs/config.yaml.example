# System-wide Configuration for Agentic Notebook Marker
# These are fallback defaults when not specified in assignment overview.md
#
# Copy this file to config.yaml and customize for your setup.

# Default LLM Provider (required)
# Options: claude, gemini, codex
default_provider: claude

# Default Model (optional - leave empty to use provider's built-in default)
# Examples by provider:
#   Claude: claude-sonnet-4, claude-opus-4, claude-haiku-4
#   Gemini: gemini-2.5-pro, gemini-2.5-flash
#   Codex: gpt-5.1, gpt-4o
default_model:

# Parallel execution settings
max_parallel: 4

# Batch processing settings
# Delay (in seconds) between assignments during batch runs
batch_delay: 2

# Logging settings
verbose: true
